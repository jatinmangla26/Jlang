#include<bits/stdc++.h>
using namespace std;
#pragma once
enum class charClass {
    INVALID = -1,
    ALPHA = 0,
    NUMERIC,
    DOT,
    DELIM,
    EQU,
    LOGIC,
    ARITH,
    AND,
    OR,
    WHITESPACE,
    QUOTE,
    ENDOFFILE,
};
